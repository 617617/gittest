{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"Workflow shortcuts: for temporal-phase, the user only needs to remember /temporal-phase-start -- it diagnoses state and emits the next action (including copy-paste text for the Codex side on Host B). Workflow watchers available (each idempotent, each only touches its own workflow): /bk-watch (Blue-K v0.10 testkit), /temporal-phase-watch (temporal-phase). Multiple workflows can run in parallel. workflows/_active.md is informational only and does not gate anything.\"}}}'"
          }
        ]
      }
    ]
  }
}
